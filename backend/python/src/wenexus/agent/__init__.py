"""
agent - LangGraph agent module for WeNexus platform.

Provides a stateful roundtable facilitator agent built on LangGraph.

Depends: langgraph, langchain_openai, config
Consumers: langgraph CLI (via langgraph.json)
"""
