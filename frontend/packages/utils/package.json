{
  "name": "@wenexus/utils",
  "version": "1.0.0",
  "description": "Shared utility functions for WeNexus frontend apps",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "echo 'Utils build: skipped (no source yet)'",
    "dev": "tsup --watch",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Utils test: skipped (no source yet)'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "date-fns": "^2.30.0",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "@types/lodash": "^4.14.0",
    "typescript": "^5.2.0",
    "tsup": "^7.2.0",
    "jest": "^29.7.0"
  }
}
