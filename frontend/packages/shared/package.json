{
  "name": "@wenexus/shared",
  "version": "1.0.0",
  "description": "Shared hooks, configs, and cross-cutting concerns for WeNexus frontend",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "echo 'Shared build: skipped (no source yet)'",
    "dev": "tsup --watch",
    "lint": "echo 'Shared lint: skipped (no tsconfig)'",
    "typecheck": "echo 'Shared typecheck: skipped (no source)'",
    "test": "echo 'Shared test: skipped (no source yet)'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@wenexus/types": "workspace:*",
    "@wenexus/utils": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "tsup": "^7.2.0",
    "jest": "^29.7.0"
  }
}
