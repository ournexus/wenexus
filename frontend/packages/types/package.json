{
  "name": "@wenexus/types",
  "version": "1.0.0",
  "description": "Shared TypeScript type definitions for WeNexus domain models and APIs",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "echo 'Types build: skipped (no source yet)'",
    "dev": "tsup --watch",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "echo 'Types test: skipped (no source yet)'",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "tsup": "^7.2.0",
    "jest": "^29.7.0"
  }
}
